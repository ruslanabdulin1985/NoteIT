extends layout

block layout-content
  main 
    div.centred
      h1 My sinppets
    div.centred.padded
      div#list
        div.centred
        button#addbtn ADD
        ul
        each val in snippets.length ? snippets : ['There are no snippets']
          //- li= val.name
          p.singlerow 
            a(href="/snippets/"+val.name) #{val.name} 
            label author:  #{val.user}
    
    //- each val in #{snippets}.length ? values : ['There are no values']
    //- li= val
    //- ul
    //- each val in #{snippets}
    //-   li= val